<UserControl x:Class="WpfSnakeGameVGr.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfSnakeGameVGr"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Border BorderBrush="DarkRed" BorderThickness="4">
        <Grid Background="Black">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Canvas Name="drawingCanvas" Background="#FF1C1C1C" 
                    Grid.Column="1" HorizontalAlignment="Stretch" MaxWidth="642" MaxHeight="422">
            </Canvas>
        </Grid>
    </Border>
</UserControl>
